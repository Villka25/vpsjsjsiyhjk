name: Ubuntu Desktop (noVNC via Cloudflare)

on:
  workflow_dispatch:

jobs:
  desktop:
    runs-on: ubuntu-latest
    steps:
      - name: Install desktop, VNC, noVNC, cloudflared
        run: |
          sudo apt update
          sudo apt install -y xfce4 xfce4-goodies tigervnc-standalone-server git python3 python3-pip
          pip3 install websockify
          git clone https://github.com/novnc/noVNC.git ~/noVNC
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared-linux-amd64
          sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared

      - name: Setup VNC password and xstartup
        run: |
          mkdir -p ~/.vnc
          echo "secret" | vncpasswd -f > ~/.vnc/passwd
          chmod 600 ~/.vnc/passwd
          cat > ~/.vnc/xstartup << 'EOF'
          #!/bin/sh
          unset SESSION_MANAGER
          unset DBUS_SESSION_BUS_ADDRESS
          exec startxfce4
          EOF
          chmod +x ~/.vnc/xstartup

      - name: Start VNC server
        run: |
          vncserver :1 -geometry 1280x720 -depth 24 -localhost yes
          sleep 3

      - name: Start noVNC (websockify)
        run: |
          websockify --web ~/noVNC 8080 localhost:5901 &
          sleep 3

      - name: Start Cloudflare Tunnel and wait for URL
        run: |
          cloudflared tunnel --url http://localhost:8080 --protocol http2 2>&1 | tee /tmp/cf.log &
          sleep 5
          for i in {1..30}; do
            URL=$(grep -o 'https://[a-zA-Z0-9\-]*\.trycloudflare\.com' /tmp/cf.log | head -1)
            if [ -n "$URL" ]; then
              echo "============================================================"
              echo "✅ Cloudflare Tunnel is ready!"
              echo ""
              echo "🌐 Open in your browser:"
              echo "   $URL/vnc.html?password=secret"
              echo ""
              echo "🔑 Password: secret"
              echo "============================================================"
              break
            fi
            echo "Waiting for tunnel (attempt $i)..."
            sleep 2
          done
          if [ -z "$URL" ]; then
            echo "❌ Failed to get tunnel URL. Check logs above."
            exit 1
          fi
          sleep 6h
