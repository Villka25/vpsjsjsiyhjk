name: Build EXE

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install MinGW-w64
        run: |
          choco install mingw -y
          echo "C:\ProgramData\chocolatey\lib\mingw\tools\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
      - name: Compile
        run: |
          g++ -static -O2 main.cpp -lgdi32 -lcomdlg32 -o drawing_printer.exe
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: drawing_printer
          path: drawing_printer.exe
